services:
  postgres-test:
    image: postgres:18-alpine
    container_name: cryptosharia-api-postgres-test
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: cryptosharia_api_test
    ports:
      - '5433:5432'
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U postgres -d cryptosharia_api_test']
      interval: 1s
      timeout: 5s
      retries: 30

  # redis-test:
  #   image: redis:8-alpine
  #   container_name: cryptosharia-api-redis-test
  #   ports:
  #     - '6380:6379'
  #   healthcheck:
  #     test: ['CMD', 'redis-cli', 'ping']
  #     interval: 1s
  #     timeout: 5s
  #     retries: 30
